- hosts: all
  become: yes
  become_method: sudo
  gather_facts: false
  tasks:
  - name: Set authorized key taken from file
    authorized_key:
      user: r09afh
      state: present
      key: "{{ lookup('file', '/home/r09afh/.ssh/id_rsa.pub') }}"
